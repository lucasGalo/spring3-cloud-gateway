apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: spring3-cloud-gateway
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/lucasGalo/spring3-cloud-gateway.git'
    targetRevision: main
    path: k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
